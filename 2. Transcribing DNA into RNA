# Simplest way
DNA_sample = "ACGT"
print(DNA_sample.replace("T","U"))

# Transcription with a fuction method
DNA_sample = "ACGT"

def transcription(seq):
    return seq.replace("T", "U")

print(transcription(DNA_sample))
